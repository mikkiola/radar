# SPEC.md

No active `/spec` focus right now.

Full decision history for the closed SPEC A / A.5 / A.6 / E / C / D
blocks that previously accumulated in this file now lives in
`docs/adr/0007` through `docs/adr/0012` respectively. SPEC B
(Repository Discoverability) has no dedicated ADR — see `git log` for
its record.

Per `docs/CONSTITUTION.md`, this file has exactly one location (the
repository root) and is never deleted — it's the target the next
`/spec` session overwrites wholesale once a new task becomes the active
focus. History of what it previously contained is retrieved via
`git log -- SPEC.md`, not preserved as a stub inside the file itself —
the same rule article-pipeline's `docs/CONSTITUTION.md` documents for
its own SPEC.md (`## SPEC.md's status`), though article-pipeline's file
currently holds a live, in-progress spec rather than an idle state like
this one, so there was no real "idle SPEC.md" example there to mirror
directly — this file's wording is Radar's own, not a copy of a
precedent that doesn't exist.
